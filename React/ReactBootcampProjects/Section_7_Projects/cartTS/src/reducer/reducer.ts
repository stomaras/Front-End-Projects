import { ItemState } from "../models/models";
import { CLEAR_CART, INCREASE, DECREASE, LOADING, DISPLAY_ITEMS, REMOVE } from "../actions/actions";


const reducer = (state:ItemState, action:string) => {
    return state;
}


export default reducer