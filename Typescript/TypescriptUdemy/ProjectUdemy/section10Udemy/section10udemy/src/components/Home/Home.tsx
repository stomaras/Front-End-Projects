import React from 'react';

import Card from '../UI/Card/Card';
import classes from './Home.module.css';

export const Home = (props:any) => {
  return (
    <Card className={classes.home}>Home</Card>
  )
}
